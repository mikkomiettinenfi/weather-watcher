import { ComponentFixture, TestBed } from '@angular/core/testing';

import { WeatherWatcherDashboardComponent } from './weather-watcher-dashboard.component';
import {HttpClientTestingModule} from "@angular/common/http/testing";

/*
describe('WeatherWatcherDashboardComponent', () => {
  let component: WeatherWatcherDashboardComponent;
  let fixture: ComponentFixture<WeatherWatcherDashboardComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [
        HttpClientTestingModule
      ],
      declarations: [
        WeatherWatcherDashboardComponent
      ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(WeatherWatcherDashboardComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
